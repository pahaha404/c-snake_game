// =============================================================================
// PoisonItem.h
// -----------------------------------------------------------------------------
// 독 아이템(Poison Item, 셀 값 6) 의 생성 함수 선언.
// 
// 먹으면 꼬리 두 칸을 제거한 뒤 새 머리를 추가하는 방식으로
// 최종적으로 Snake의 몸 길이가 1칸 줄어든다. 줄어든 결과 길이가 3 미만이 되면
// 즉시 게임오버 처리된다(획득 처리는 snake.cpp 의 move_Snake() 에서 한다).
// =============================================================================
#ifndef POISONITEM_H
#define POISONITEM_H

class PoisonItem {
public:
    void generate(const int stage_num);
};

#endif