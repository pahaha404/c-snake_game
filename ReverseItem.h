// =============================================================================
// ReverseItem.h
// -----------------------------------------------------------------------------
// 방향 혼란 아이템(Reverse Direction Item, 셀 값 8) 의 생성 함수 선언.
//
// - 등장 조건 : 3 스테이지(내부 stage_num == 2) 이상부터만 등장하여,
//               후반 난이도 상승 요소로 동작한다.
// - 획득 효과 : 10초 동안 진행방향에 "수직" 인 두 화살표 키의 기능을 서로
//               맞바꾼다. 진행방향이 바뀌면 기준도 매 틱 재계산된다
//               (snake.cpp 의 update_reverse_keys() 참조).
// =============================================================================
#ifndef REVERSEITEM_H
#define REVERSEITEM_H

// 3 스테이지 이상인 경우에만 빈칸에 Reverse Item 을 배치한다.
// 그 이전 스테이지에서는 아무 일도 하지 않고 즉시 반환한다.
void generate_reverse_item(int stage_num);

#endif