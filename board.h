// =============================================================================
// board.h
// -----------------------------------------------------------------------------
// 맵 데이터 객체화(Board) 클래스 정의 헤더.
// 30x30 크기의 4개 스테이지별 맵 정보를 관리하며,
// 전체 게임 요소가 동일한 map 접근 방식을 사용할 수 있도록 구성합니다.
// =============================================================================
#ifndef BOARD_H
#define BOARD_H

class Board {
public:
    int map[4][30][30];
    Board();
};

extern Board board_obj;

// 전체 게임 요소가 map 이름으로 Board의 맵 배열에 접근할 수 있도록 하는 매크로
#define map board_obj.map

#endif
